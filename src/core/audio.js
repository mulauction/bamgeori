// ══════════════════════════════════════════════════════════════
//  audio.js — 사운드 인터페이스 (현재는 무음 플레이스홀더)
//  데모에는 사운드가 없었다. 게임 모듈이 ctx.audio.play('win') 처럼
//  호출할 수 있도록 인터페이스만 미리 열어 둔다. 2단계에서 실제 구현.
// ══════════════════════════════════════════════════════════════

export const audio = {
  /** @param {string} _name 사운드 키 (예: 'win', 'lose', 'coin') */
  play(_name) {
    /* no-op: 추후 오디오 에셋 로딩 시 교체 */
  },

  // ⚠️ 앱 출시 규칙: 실제 오디오는 반드시 유저 제스처(탭 등) 콜백 안에서
  // AudioContext.resume()/재생을 시작해야 한다(모바일 웹뷰 자동재생 차단).
  // 예) 첫 베팅 버튼 클릭 시 unlock() 호출 → 이후 play() 사용.
  unlock() {
    /* no-op: 추후 AudioContext 생성/resume 지점 */
  },
};
